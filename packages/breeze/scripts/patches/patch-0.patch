diff --git i/packages/tailwindcss/src/caslon.ts w/packages/tailwindcss/src/caslon.ts
new file mode 100644
index 00000000..5be21a7f
--- /dev/null
+++ w/packages/tailwindcss/src/caslon.ts
@@ -0,0 +1,43 @@
+import type { Candidate } from "./candidate"
+
+const REGEX = /^(([a-z]+)|(\d+))(-(\d{1,3})-(\d{1,3}))?(-(\d{1,2}|100))?$/i
+
+const parseNumber = (value: string | undefined) =>
+  value === undefined ? undefined : parseInt(value, 10)
+
+export const isColor = (
+  variable: string,
+): boolean => {
+  const array = REGEX.exec(variable)
+
+  if (array === null) {
+    return false
+  }
+
+  const colorString = array[2] as string | undefined
+  const colorNumber = colorString === undefined ? parseNumber(array[3])! : undefined
+  const color = (colorString ?? colorNumber)
+
+  // TODO: named colors, without lightness/chroma/alpha
+  if (color === undefined) {
+    return false
+  }
+
+  const lightness = array[5]
+  const chroma = lightness === undefined ? undefined : array[6] as string | undefined
+
+  if (lightness === undefined || chroma === undefined) {
+    return false
+  }
+
+  return true
+}
+
+
+export const resolveCepheusColor = (value: Exclude<Extract<Candidate, { kind: 'functional' }>['value'], null>) => {
+  if (value.kind === 'named' && isColor(value.value)) {
+    return `var(---color-${value.value})`
+  }
+
+  return null
+}
diff --git i/packages/tailwindcss/src/utilities.ts w/packages/tailwindcss/src/utilities.ts
index 2f121d34..05eaa814 100644
--- i/packages/tailwindcss/src/utilities.ts
+++ w/packages/tailwindcss/src/utilities.ts
@@ -26,6 +26,8 @@ import { replaceShadowColors } from './utils/replace-shadow-colors'
 import { segment } from './utils/segment'
 import * as ValueParser from './value-parser'
 
+import { resolveCepheusColor } from './caslon'
+
 const IS_VALID_STATIC_UTILITY_NAME = /^-?[a-z][a-zA-Z0-9/%._-]*$/
 const IS_VALID_FUNCTIONAL_UTILITY_NAME = /^-?[a-z][a-zA-Z0-9/%._-]*-\*$/
 
@@ -70,13 +72,13 @@ type CompileFn<T extends Candidate['kind']> = (
   value: Extract<Candidate, { kind: T }>,
 ) => AstNode[] | undefined | null
 
-interface SuggestionGroup {
+export interface SuggestionGroup {
   supportsNegative?: boolean
   values: (string | null)[]
   modifiers: string[]
 }
 
-type SuggestionDefinition =
+export type SuggestionDefinition =
   | string
   | {
       supportsNegative?: boolean
@@ -227,7 +229,7 @@ export function asColor(
  * The values `inherit`, `transparent` and `current` are special-cased as they
  * are universal and don't need to be resolved from the theme.
  */
-function resolveThemeColor<T extends ThemeKey>(
+export function resolveThemeColor<T extends ThemeKey>(
   candidate: Extract<Candidate, { kind: 'functional' }>,
   theme: Theme,
   themeKeys: T[],
@@ -259,13 +261,29 @@ function resolveThemeColor<T extends ThemeKey>(
     }
     default: {
       value = theme.resolve(candidate.value!.value, themeKeys)
+
+      if (value === null) {
+        value = resolveCepheusColor(candidate.value!)
+      }
+
       break
     }
   }
 
+
   return value ? asColor(value, candidate.modifier, theme) : null
 }
 
+export type UtilityDescription = {
+  supportsNegative?: boolean
+  supportsFractions?: boolean
+  themeKeys?: ThemeKey[]
+  defaultValue?: string | null
+  handleBareValue?: (value: NamedUtilityValue) => string | null
+  handleNegativeBareValue?: (value: NamedUtilityValue) => string | null
+  handle: (value: string, dataType: string | null) => AstNode[] | undefined
+}
+
 export function createUtilities(theme: Theme) {
   let utilities = new Utilities()
 
@@ -361,16 +379,6 @@ export function createUtilities(theme: Theme) {
     })
   }
 
-  type UtilityDescription = {
-    supportsNegative?: boolean
-    supportsFractions?: boolean
-    themeKeys?: ThemeKey[]
-    defaultValue?: string | null
-    handleBareValue?: (value: NamedUtilityValue) => string | null
-    handleNegativeBareValue?: (value: NamedUtilityValue) => string | null
-    handle: (value: string, dataType: string | null) => AstNode[] | undefined
-  }
-
   /**
    * Register a functional utility class like `max-w-*` that maps to tokens in the
    * user's theme.
@@ -5709,6 +5717,14 @@ export function createUtilities(theme: Theme) {
     },
   ])
 
+  Object.assign(utilities, {
+    references: {
+      functionalUtility,
+      staticUtility,
+      suggest
+    }
+  })
+
   return utilities
 }
 
diff --git i/packages/tailwindcss/src/variants.ts w/packages/tailwindcss/src/variants.ts
index 701f08c0..30c56224 100644
--- i/packages/tailwindcss/src/variants.ts
+++ w/packages/tailwindcss/src/variants.ts
@@ -1132,7 +1132,7 @@ export function createVariants(theme: Theme): Variants {
   staticVariant('ltr', ['&:where(:dir(ltr), [dir="ltr"], [dir="ltr"] *)'])
   staticVariant('rtl', ['&:where(:dir(rtl), [dir="rtl"], [dir="rtl"] *)'])
 
-  staticVariant('dark', ['@media (prefers-color-scheme: dark)'])
+  // staticVariant('dark', ['@media (prefers-color-scheme: dark)'])
 
   staticVariant('starting', ['@starting-style'])
 
