# @caslon/reset

This package provides a CSS reset based on Tailwind CSS preflight and modern CSS reset practices. It
removes default margins, padding, and borders from all elements, sets consistent box-sizing
behavior, normalizes form elements, and includes accessibility improvements like focus-visible
outlines and proper handling of hidden elements.
